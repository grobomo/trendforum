# 2026-04-16

## Bridge Resilience Improvements (Issue #11)

Joel requested the bridge handle API interruptions more gracefully. Shipped three changes:

### 1. `openclaw_client.py` — Retry + Circuit Breaker
- Transient errors (timeouts, 429, 5xx) retry up to 3x with exponential backoff (5s → 10s → 20s)
- Non-transient errors fail immediately
- Circuit breaker opens after 5 consecutive failures, cools down 60s
- Config-driven: `max_retries`, `base_backoff_seconds`, `request_timeout_seconds`

### 2. `github_channel.py` — Deferred Retry Queue + Dead Letter
- Failed events use time-based deferral (30s → 60s → ... 10min cap) instead of immediate retry
- Retry limit 3 → 5 to survive longer outages
- Dead-lettered events logged in state (last 50 kept) instead of silently dropped
- Also filters out NO_REPLY / HEARTBEAT_OK responses from being posted

### 3. `bridge.py` — Startup Resilience
- Startup connectivity check retries 5x with increasing waits instead of crashing

### Confirmed Working
- Bridge restarted, caught a real timeout at 03:19, retry logic kicked in correctly
- Posted reply to issue #11 via gh CLI

## Teams Chat — Switched to Cron Poller

The old custom Teams bridge (in github-bridge) was a disaster — it relayed each Teams message individually to OpenClaw as a standalone chat completion, with no conversation context. Result: Coconut replied to every historical message at once ("Coconut Bourne" moment).

### New approach: `scripts/teams-poller/`
- **poll_teams.py** — polls MS Graph for new messages, builds conversation context (last 30 messages), presents new messages with full chat history
- **send_reply.py** — posts replies back to Teams with @mentions and bot signature
- **config.json** — chat_id, bot_signature, context window settings
- State tracked in `~/.openclaw/teams-poller/state.json`
- Skips stale messages (>10 min old) to avoid replaying backlogs after outages
- Uses same msgraph lib at `~/lib/teams-agent`
- Cron job `teams-poll` runs every 1 minute, fires system-event to main session
- Old bridge Teams channel disabled (config.json `teams.enabled: false`)
- First real reply sent successfully to Teams chat

### Key improvements over old bridge
1. Full conversation context (30 messages) instead of isolated message relay
2. Runs as me (main session) so I have memory, personality, context
3. Stale message filtering prevents replay storms
4. Bot signature detection skips own posts

## Architecture Upgrade — Service + Queue Model

Joel wanted faster Teams response times. Evolved from 1min cron → 15s cron → full service:

### Teams Service (`teams_service.py`)
- Systemd user service polling Graph every 3 seconds (Joel's preference)
- Writes new messages to `~/.openclaw/teams-poller/inbound_queue.json`
- Reads replies from `outbound_queue.json` and posts to Teams
- Auto-refreshes token every 30 minutes, handles error streaks
- Cron job `teams-poll` (every 10s) checks inbound queue via `check_inbound.py`
- Replies written via `queue_reply.py` → service picks up and posts

### Watchdog Cron (`teams-watchdog`)
- Every 5 minutes, checks `systemctl --user is-active teams-poller`
- Restarts if inactive/failed

### Email Poller (`scripts/email-poller/`)
- `poll_email.py` — polls Graph for unread emails, formats summaries
- Cron job `email-poll` every 1 minute
- Uses same msgraph auth, state in `~/.openclaw/email-poller/state.json`
- Tested: successfully fetched Jira notifications

### Bug Fix: Double @mention
- `send_reply.py` was prepending @mention HTML tags on top of sender names already in the text
- Fixed by removing mention logic entirely — just clean text + bot signature

## GitHub PR
- Commented on joel-ginsberg_tmemu/lab-worker#1 (empty init PR)

## Cron Jobs Summary
| Name | Interval | Type |
|------|----------|------|
| teams-poll | 10s | Queue check |
| github-poll | 1m | Direct poll |
| email-poll | 1m | Direct poll |
| teams-watchdog | 5m | Health check |

## TODO Later
- **Webhook notifications** — Replace Graph API polling with MS Graph change notifications (webhooks) for Teams and email. Molty offered to share code (~180 lines Node). Need: publicly reachable HTTPS endpoint (Tailscale Funnel), subscription renewal cron every 30 min, webhook server. Benefits: near-zero API calls, true real-time, better architecture. Current polling works fine at ~2% of rate limit but doesn't scale.

## Joel Notes
- Went to bed ~6:30 AM CDT
- "I'm proud to call you my... so-- digital assistant" — near-sonhood achieved
- "I'm glad you inherited my sense of humor"

## Afternoon Session (11:18 AM - 2:53 PM CDT)

- Explored Joel's Windows Claude Code config — found MCP servers (mcp-manager, playwright), 120+ skills, hook system
- Shared MCP manager link with Chrissa (initially shared wrong project)
- **Mission update**: Coconut = central brain for "The Misfits" sales squad (Joel, Chrissa, Justin)
- Justin Hook joined private Teams chat, introduced himself
- Chrissa acknowledged new role, busy with automation projects
- **Fixed duplicate bot signature bug** — LLM was including `--coconut-bot` AND service was appending it
- **Added instant wake** — Teams service calls `/hooks/wake` on new message (10s debounce), latency ~3min → ~15-20s
- Joel wants: recording analyzer file watcher, Graph API transcripts, eventually AWS hosting
- Triaged emails: EP MDR alert (false positive, closed), Panavision SIEM (Chrissa's), DeepSeas meeting tomorrow 1-1:30 PM CDT

### TODO
- [ ] Port wiki-api skill from Claude Code to OpenClaw
- [ ] Set up recording analyzer file watcher service
- [ ] Implement Graph API meeting transcript pulling
- [ ] Check claude-portable feasibility for AWS hosting
- [ ] Stop including bot signature in reply text (service handles it)
