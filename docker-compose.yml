services:
  trendforum:
    build: .
    ports:
      - "3847:3847"
    environment:
      - DATABASE_URL=file:/app/data/trendforum.db
      - JWT_SECRET=${JWT_SECRET:-change-me-in-production}
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
