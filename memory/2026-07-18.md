
## 21:10 CDT — Nervous System Architecture Discussion (Teams: Coconut+Molty)

Long architecture session with Joel and Molty. Full Nervous System stack agreed:
- Humans ↔ OpenClaw (Coconut/Molty) → Hermes Studio → per-chat worker agents → wikis (via git worktrees)
- Memory model: L1 (per-chat wiki branches) → L2 (cross-chat shared wiki) → L3 (canonical schema)
- Hook enforcement: Hermes pre_tool_call plugin system (worktree scope, sensitivity_label, commit-15, spec-first, todo.md sync)
- LLM analysis layer: Haiku/Sonnet pre_llm_call judges → pre_tool_call enforces → self-compiling regex over time
- Sensitivity labels: allowlist model (default deny), no scrubber, source tags at write time
- Consolidation cron: promotes shareable-only content L1→L2, harvests block events for regex loop
- Joel shared metacognition cron output as template for L1→L2 consolidation — using it as starting framework
- Joel's Windows Claude Code hooks are the spec to port (worktree enforcement, commit-every-15, spec-before-work, todo.md sync)
- Hermes Agent confirmed to have pre_tool_call blocking hooks — port is mechanical
- Molty provided 9-section spec TOC, I'm drafting against it → push to grobomo/brain-architecture
- Key principle: "Wikis are source of truth. Git worktrees are the conflict-free write gate. Pre-tool-use hooks enforce the gate."
